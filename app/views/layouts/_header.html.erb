<div class ="fixed">
<nav class="top-bar" data-topbar>
        <ul class="title-area">
          <li class="name">
            <h1>
              <% if user_signed_in? %>
                <%=link_to 'Rio Runner', user_path(current_user.id) %>
                <% else %>
                <%=link_to 'Rio Runner', root_path %>
              <% end %>
            </h1>
          </li>
          <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
        </ul>

        <section class ="top-bar-section">
        <ul class ="right">

          <% if user_signed_in? %>
          <li class="has-dropdown">
            <%= link_to current_user.first_name, riorunner_path%>
            <ul class="dropdown">
              <li> <%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
              <li><%= link_to('My Account', edit_user_registration_path) %></li>
              <li><a href="#">first link in dropdown</a></li>
              <li><a href="#">second link in dropdown</a></li>
              <li><a href="#">third link in dropdown</a></li>
            <%= link_to current_user.first_name, user_path(current_user.id)%>
            <ul class="dropdown">
              <li> <%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
              <li><%= link_to('My Account', edit_user_registration_path) %></li>
            </ul>
          </li>
          <% else %>
              <li><%= link_to('Login', new_user_session_path)%> </li>
              <li><%= link_to('Register', new_user_registration_path)  %> </li>
          <%end%>
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "Help", help_path %></li>
        </ul>
        </section>
</nav>
</div>