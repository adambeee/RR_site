<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>

    <% if user_signed_in? %>
        <%=link_to 'Rio Runner', myriorunner_path(current_user.id), class: "navbar-brand" %>
    <% else %>
        <%=link_to 'Rio Runner', root_path, class: "navbar-brand" %>
    <% end %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
          <li class="dropdown">
            <a href="" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.first_name %><% if current_user.notifications.count >= 1%>&nbsp;<span class="badge"><%=current_user.notifications.count%></span><% end %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><a href="<%= myriorunner_path(current_user.id)%>">My Rio Runner<% if current_user.notifications.count >= 1%>&nbsp;<span class="badge"><%=current_user.notifications.count%></span><% end %></a></li>
              <li><%= link_to('My Account', user_path(current_user.id)) %></li>
              <li><%= link_to('Account Settings', edit_user_registration_path) %></li>
              <li><a href="#">Something else here</a></li>
              <li><a href="#">first link in dropdown</a></li>
              <li><a href="#">second link in dropdown</a></li>
              <li><a href="#">third link in dropdown</a></li>
              <li class="divider"></li>
              <li> <%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
            </ul>
          </li>
      <% else %>
          <li><a href = "#" data-toggle ="modal" data-target = "#myModal">Maybe a sign in modal?</a></li>
          <li><%= link_to('Login', new_user_session_path)%> </li>
          <li><%= link_to('Register', new_user_registration_path)  %> </li>
      <%end%>
      <li><%= link_to "Home", root_path, class: "active" %></li>
      <li><%= link_to "Help", help_path %></li>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>
